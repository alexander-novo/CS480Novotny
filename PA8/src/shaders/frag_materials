#version 330 core

// Interpolated values from the vertex shaders
in vec2 uvCoord;

uniform sampler2D gSampler;

// Output data
out vec4 frag_color;

void main(void) {
    vec4 MaterialDiffuseTexture2d = texture2D(gSampler, uvCoord.st);
    frag_color.rgba = MaterialDiffuseTexture2d.rgba;
}